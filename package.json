{
  "name": "user-registration-app-with-mongodb",
  "version": "1.0.0",
  "description": "Mini app de registro de usuarios con MongoDB",
  "main": "backend/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js ",
    "install-deps": "npm install",
    "setup": "npm install && echo 'Dependencias instaladas. Ejecutar: npm start'"
  },
  "engines": {
    "node": "20.x"
  },
  "keywords": [
    "nodejs",
    "mongodb",
    "express",
    "registration"
  ],
  "author": "Claudio Candia",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.6.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
